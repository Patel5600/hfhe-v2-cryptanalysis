# powg_B Discrete Log Analysis

Parameter $B = 337$.
The base group cyclic order is $ord(g) = 337$.
Therefore:
$$g^B = g^{337} = 1 \pmod p$$
$g^B$ is identically the group identity element.
Index exponents $idx \in [0, 16383]$ are public metadata. The discrete logarithm is algebraically trivial and reveals zero private key entropy.\n