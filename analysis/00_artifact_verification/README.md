# Phase 0 — Artifact Verification

Verify that the challenge artifacts parse correctly and match expected structure.

## Scripts
- secret_ct_structure.py — Parse secret.ct, count CT objects, verify H digest
- pk_bin_structure.py (stub) — Parse pk.bin, print parameter table

## Status: CLOSED
Both artifacts parse without error and match expected structural invariants.
